<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.decoderssquad.medipastprojectone">

    <application
        android:allowBackup="true"
        android:icon="@drawable/medicinelist"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DoctorList" />
        <activity android:name=".PressList" />
        <activity android:name=".Add" />
        <activity android:name=".SingleList" />
        <activity android:name=".Addmedical"></activity>
    </application>

</manifest>